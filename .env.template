# Online Mahalla — Environment Template
#
# Secrets are stored in 1Password vault "Online-mahalla".
# Real .env file generation:
#
#   op inject -i .env.template -o .env.local
#
# Run-time (no .env file needed):
#
#   op run --env-file=.env.template -- npm run dev
#
# Add new secrets here using op:// references — never inline real values.

# === GitHub (auth via gh CLI, this is just reference) ===
GITHUB_TOKEN="op://Online-mahalla/GitHub/credential"

# === Future: Database (Supabase/Postgres) ===
# DATABASE_URL="op://Online-mahalla/Database/url"
# DATABASE_PASSWORD="op://Online-mahalla/Database/password"

# === Future: Auth (NextAuth/Clerk/Auth0) ===
# NEXTAUTH_SECRET="op://Online-mahalla/NextAuth/secret"
# NEXTAUTH_URL="http://localhost:3000"
# GOOGLE_CLIENT_ID="op://Online-mahalla/Google OAuth/client_id"
# GOOGLE_CLIENT_SECRET="op://Online-mahalla/Google OAuth/client_secret"

# === Future: Maps (Google Maps for Uzbekistan map) ===
# NEXT_PUBLIC_GOOGLE_MAPS_KEY="op://Online-mahalla/Google Maps/credential"

# === Future: Email (Resend/SendGrid) ===
# RESEND_API_KEY="op://Online-mahalla/Resend/credential"
# EMAIL_FROM="no-reply@online-mahalla.uz"

# === Future: Analytics ===
# NEXT_PUBLIC_POSTHOG_KEY="op://Online-mahalla/PostHog/credential"
